---
import { getCollection } from 'astro:content'
import { Picture } from 'astro:assets'
import BookCallButton from '@/components/buttons/BookCallButton.astro'
import HireButton from '@/components/buttons/HireButton.astro'
import PageLayout from '@/layouts/PageLayout.astro'

const projects = await getCollection('projects')
const allImages = projects.flatMap(entry =>
  entry.data.images.map(filename => ({ projectId: entry.id, filename })),
)
---

<PageLayout title="Explore — aoie studio" hideFooter noindex>
  <section class="explore page-wrap">
    <nav class="explore-nav" aria-label="Explore">
      <a href="/" class="explore-link">Studio</a>
      <a href="/works" class="explore-link">Works</a>
      <a
        href="https://www.cosmos.so/aoiestudio"
        class="explore-link explore-link--lab"
        target="_blank"
        rel="noopener"
        >Laboratory<svg
          class="explore-star"
          width="18"
          height="18"
          viewBox="0 0 18 18"
          fill="currentColor"
          xmlns="http://www.w3.org/2000/svg"
          aria-hidden="true"
        >
          <path
            d="M6.08671 18L2.49711 15.9034L5.6185 11.25H0V6.75L5.6185 6.80114L2.49711 2.14773L6.08671 0L9.05202 4.96023L12.0173 0L15.6069 2.14773L12.3815 6.80114L18 6.75V11.25H12.3815L15.6069 15.9034L12.0173 18L9.05202 13.142L6.08671 18Z"
          ></path>
        </svg></a
      >
    </nav>

    <div class="explore-media-wrap">
      <div class="explore-media" id="explore-media">
        {
          allImages.map((item, index) => (
            <div
              class:list={['explore-media__slide', index === 0 && 'is-active']}
              data-explore-img
            >
              <Picture
                src={`projects/${item.projectId}/images/${item.filename}`}
                formats={['avif', 'webp']}
                width={640}
                height={360}
                sizes="640px"
                loading={index === 0 ? 'eager' : 'lazy'}
                alt=""
              />
            </div>
          ))
        }
      </div>
    </div>

    <div class="explore-actions">
      <HireButton class="explore-link" />
      <BookCallButton class="explore-link" />
    </div>
  </section>
</PageLayout>

<script>
  function init() {
    const stage = document.querySelector<HTMLElement>('#explore-media')
    if (!stage) {
      return
    }
    const imgs = [...stage.querySelectorAll<HTMLElement>('[data-explore-img]')]
    if (imgs.length < 2) {
      return
    }

    let idx = 0
    const tick = () => {
      imgs[idx]?.classList.remove('is-active')
      let next = idx
      while (next === idx) {
        next = Math.floor(Math.random() * imgs.length)
      }
      idx = next
      imgs[idx]?.classList.add('is-active')
    }
    window.setInterval(tick, 1000)
  }

  init()
</script>

<style>
  .explore {
    padding-top: var(--space-2xl);
    padding-bottom: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
    min-height: 100vh;
    box-sizing: border-box;
  }

  .explore-nav {
    display: flex;
    flex-direction: column;
  }

  .explore-link {
    font-family: var(--font-family);
    font-weight: 600;
    font-size: 55px;
    line-height: 1;
    letter-spacing: var(--letter-spacing-heading);
    font-feature-settings: 'ss03', 'ss02';
    color: var(--color-text-inverse);
    background: transparent;
    border: 0;
    padding: 0;
    text-align: left;
    cursor: pointer;
    text-decoration: none;
  }

  .explore-link:hover {
    color: var(--color-text);
  }

  .explore-link--lab {
    position: relative;
    align-self: flex-start;
  }

  .explore-star {
    position: absolute;
    left: 100%;
    top: 0;
    margin-left: 5px;
    margin-top: 5px;
    color: var(--color-text-secondary);
    animation: explore-star-spin 12s linear infinite;
  }

  @keyframes explore-star-spin {
    to {
      transform: rotate(360deg);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .explore-star {
      animation: none;
    }
  }

  .explore-media-wrap {
    display: grid;
    grid-template-columns: repeat(var(--cols), 1fr);
    column-gap: var(--col-gap);
  }

  .explore-media {
    position: relative;
    grid-column: 1 / 3;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: var(--color-surface);
  }

  .explore-media__slide {
    position: absolute;
    inset: 0;
    opacity: 0;
  }

  .explore-media__slide.is-active {
    opacity: 1;
  }

  .explore-media__slide :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .explore-actions {
    display: flex;
    flex-direction: column;
    margin-top: auto;
  }

  .explore-actions :global(.explore-link) {
    font-size: var(--font-size-heading);
  }
</style>
